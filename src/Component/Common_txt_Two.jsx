import React from 'react'

const Common_txt_Two = ({text,className}) => {
  return (
    <h3 className={`${className} text-[#546179] text-[24px] font-normal font-Poppins`}>{text}</h3>
  )
}

export default Common_txt_Two