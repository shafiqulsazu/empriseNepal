import React from 'react'

const Travle_Head = ({className,text}) => {
  return (
    <h3 className={`${className} text-Color text-[24px] font-semibold font-Poppins`}>{text}</h3>
  )
}

export default Travle_Head